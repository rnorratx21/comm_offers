<%= render 'admin/nav' %>

<h2>New Group</h2>

<% form_for @group, :url=>admin_groups_path  do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form', :locals => {:f => f} %>
      <%= f.submit 'Create' %>
<% end %>

